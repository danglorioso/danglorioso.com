---
interface Props {
  title: string;
  description: string;
  pubDate: string;
  tags: string[];
  category?: string;
}

const { 
  title = "Blog Post Title", 
  description = "Blog post description", 
  pubDate,
  tags = [],
  category = "Technology"
} = Astro.props;

// Format date function
const formatDate = (dateString: string) => {
  const date = new Date(dateString);
  return date.toLocaleDateString('en-US', {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
    timeZone: 'UTC'
  });
};
---

<div class="group bg-bg-card backdrop-blur-sm border border-border-primary rounded-lg flex flex-col p-4 gap-3 hover:border-border-accent transition-all duration-200 cursor-pointer">
  <!-- Post Title and Date -->
  <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-2 sm:gap-4">
    <h2 class="text-2xl font-semibold text-text-primary group-hover:text-accent-primary transition-colors flex-1">
      {title}
    </h2>
    <time datetime={pubDate} class="text-sm text-text-secondary whitespace-nowrap flex-shrink-0">
      {formatDate(pubDate)}
    </time>
  </div>

  <!-- Excerpt -->
  <p class="text-text-secondary text-md leading-relaxed">
    {description}
  </p>

  <!-- Tags -->
  <div class="flex flex-wrap gap-2">
    {tags.map((tag) => (
      <span class="px-2 py-1 bg-bg-overlay text-text-tertiary text-sm rounded-md border border-border-tertiary uppercase tracking-wide">
        {tag}
      </span>
    ))}
  </div>
</div>
